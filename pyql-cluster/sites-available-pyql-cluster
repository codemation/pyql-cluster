server {
    listen 80;
    server_name localhost
                pyql-cluster
                *.default.svc.cluster.local;

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/pyql-cluster/pyql.sock;
    }
}